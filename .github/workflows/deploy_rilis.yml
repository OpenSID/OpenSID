name: Deploy OpenSID Rilis with Ansible

on:
  release:
    types: [published, edited, prereleased]

permissions:
  contents: read

concurrency:
  group: deploy-opensid-rilis
  cancel-in-progress: true

jobs:
  deploy-production:
    # Hanya jalan kalau rilis dari branch umum & bukan prerelease,
    # serta event adalah published atau edited
    if: |
      github.event.release.target_commitish == 'umum' &&
      github.event.release.prerelease == false &&
      (github.event.action == 'published' || github.event.action == 'edited')
    name: Deploy OpenSID Rilis (Release)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Ansible Code
        uses: actions/checkout@v4

      - name: Deploy via SSH (Release)
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.CICD_SERVER_IP }}
          username: ${{ secrets.CICD_SERVER_USER }}
          key: ${{ secrets.CICD_SERVER_PRIVATE_KEY }}
          port: ${{ secrets.CICD_SERVER_PORT }}
          script: |
            set -euo pipefail
            cd ${{ secrets.CICD_SERVER_PATH }}
            ansible-playbook -i inventories/production/inventory.yml playbooks/deploy-opensid-rilis.yml --extra-vars "release_tag=${{ github.event.release.tag_name }}"

  deploy-prerelease:
    # Hanya jalan kalau rilis dari branch umum & prerelease = true,
    # serta event adalah published atau edited
    if: |
      github.event.release.target_commitish == 'umum' &&
      github.event.release.prerelease == true &&
      (github.event.action == 'published' || github.event.action == 'edited')
    name: Deploy OpenSID Rilis (Pre-Release)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Ansible Code
        uses: actions/checkout@v4

      - name: Deploy via SSH (Pre-Release)
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.CICD_SERVER_IP }}
          username: ${{ secrets.CICD_SERVER_USER }}
          key: ${{ secrets.CICD_SERVER_PRIVATE_KEY }}
          port: ${{ secrets.CICD_SERVER_PORT }}
          script: |
            set -euo pipefail
            cd ${{ secrets.CICD_SERVER_PATH }}
            ansible-playbook -i inventories/production/inventory.yml playbooks/deploy-opensid-rilis.yml --extra-vars "release_tag=${{ github.event.release.tag_name }}"
