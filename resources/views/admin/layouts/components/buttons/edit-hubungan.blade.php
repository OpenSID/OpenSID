<a
    href="{{ $url }}"
    data-remote="false"
    data-toggle="modal"
    data-target="#{{ $modalTarget }}"
    data-title="{{ $judul }}"
    title="{{ $judul }}"
    class="btn {{ $type }} btn-sm"
><i class="{{ $icon }}"></i></a>